version: '3.8'

services:
  cyxchat-server:
    build: .
    container_name: cyxchat-server
    ports:
      - "7777:7777/udp"
    restart: unless-stopped
    # Optional: healthcheck
    # healthcheck:
    #   test: ["CMD", "nc", "-u", "-z", "localhost", "7777"]
    #   interval: 30s
    #   timeout: 5s
    #   retries: 3
